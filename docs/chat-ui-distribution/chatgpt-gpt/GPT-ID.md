# Custom GPT publish record

This file records the details of the published ChatGPT Custom GPT for
the architecture-diagram skill. Filled in by the publisher after running
the steps in [`README.md`](./README.md).

## Status

`unpublished` — placeholder. Replace with `published` after Step 11 of
the setup recipe completes.

## Publish details

- **GPT id**: `<g-XXXXXXXXXX>` (from the published URL `https://chat.openai.com/g/g-XXXXXXXXXX-<slug>`).
- **Shareable link**: `<https://chat.openai.com/g/g-XXXXXXXXXX-<slug>>`.
- **Publish date**: `YYYY-MM-DD`.
- **Visibility**: `<only-me | anyone-with-link | gpt-store>`.
- **Publisher account**: `<obfuscated reference, e.g. "the project maintainer">`.

## GPT identity (as configured in GPT Builder)

- **Name**: `Architecture Diagram Assistant`
- **Description**: `Draws PlantUML architecture diagrams using official Azure / Fabric / Kubernetes / FluentUI / Devicon icons.`
- **Profile picture**: `<DALL-E-generated | custom upload | none>`.

## Conversation starters

1. `Draw an Azure 3-tier web app architecture.`
2. `Draw a Fabric data pipeline (Lakehouse + Notebook + Warehouse).`
3. `Draw a Kubernetes deployment with Pod + Service + Deployment.`
4. `Draw a DevOps pipeline diagram with GitHub + Docker + Azure.`

## Capabilities (as configured)

- Web Browsing: `OFF`
- DALL·E Image Generation: `OFF`
- Code Interpreter & Data Analysis: `OFF`

## Knowledge bundle version

- Bundle source: `chat-ui-bundle.zip` from GitHub Release `<skill-vX.Y.Z>`.
- Last uploaded: `YYYY-MM-DD`.

## Update history

| Date | Bundle release | Change |
|---|---|---|
| `YYYY-MM-DD` | `skill-vX.Y.Z` | Initial publish. |

Append a row whenever the GPT's knowledge bundle is refreshed.
